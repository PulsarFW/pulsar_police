name: release-action

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release-action:
    uses: PulsarFW/.github/.github/workflows/release-action.yml@main
    secrets: inherit
